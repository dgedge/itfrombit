# Data Policy

This directory is for small, named input fixtures that are required to reproduce
specific scripts.

Do not commit bulk simulation data, sparse matrices, `.npz`/`.npy` dumps, or raw
run directories.  Put those in an external archive or regenerate them from the
script and parameters recorded in the relevant topic README.
